spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  sql:
    init:
      mode: never
  flyway:
    enabled: false
logging:
  level:
    root: INFO
